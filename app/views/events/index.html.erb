<div class="events">
	<% @events.each do |event| %>
		<%= event.starting_at %> - <%= link_to event.title, event_path(event) %><br />
	<% end %>
</div>

<p><%= link_to "Create account", new_user_registration_path %>
	<%= link_to "Login", new_user_session_path %></p>